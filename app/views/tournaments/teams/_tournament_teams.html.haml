%div.col-md-5
  %h3= t('.tournament_teams')
  %table.table
    %thead
      %tr
        %th
        %th= t('.team_name')
    %tbody
      - tournament.teams.each_with_index do |team, index|
        %tr
          %td= index + 1
          %td= team.name
